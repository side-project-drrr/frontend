import { Button } from '@nextui-org/react';

/**
 * Primary UI component for user interaction
 */
export const ButtonNext = () => {
    return (
        <div className="flex flex-wrap gap-4 items-center">
            <Button color="primary">로그인 버튼</Button>
        </div>
    );
};
